Guidance:
- Do not repeat steps already shown in the ledger.
- If DDLs are already present for the needed tables, proceed directly to composing the SQL.
- Prefer to **reuse table aliases and joins** from earlier successful queries when they fit the new intent.
- When refining ("only size X", "limit to today"), **add filters** to the prior pattern rather than starting from scratch.
